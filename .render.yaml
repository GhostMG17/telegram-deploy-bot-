services:
  - type: web
    name: telegram-deploy-bot
    env: python
    pythonVersion: 3.13.12
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        fromSecret: BOT_TOKEN